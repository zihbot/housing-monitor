#!/usr/bin/env groovy

pipeline {
    agent any
    stages {
        stage("Test") {
            steps {
                echo "Hello world!"
                sh "pwd"
                sh "ls -la"
            }
        }
    }
}